/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Legacy-Goods-Website',
  assetPrefix: '/Legacy-Goods-Website/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
