/** @type {import('next').Next.config} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  basePath: '/Legacy-Goods-Website',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;